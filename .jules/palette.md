## 2024-05-22 - Sidebar Accessibility
**Learning:** Collapsible sidebar menus using `Collapse` need `aria-expanded` and `aria-controls` to be accessible to screen readers.
**Action:** Always link toggle buttons to their content with `aria-controls` and indicate state with `aria-expanded`.
