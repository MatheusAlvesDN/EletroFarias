// prisma/schema.prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum Role {
  ADMIN
  MANAGER
  USER
}

model User {
  id           String   @id @default(cuid())
  email        String   @unique
  passwordHash String
  role         Role     @default(USER)
  createdAt    DateTime @default(now())
}

model RewardsFidelimax {
  idVoucher String   @id
  cpf       BigInt   @db.BigInt
  value     Decimal  @db.Decimal(14, 2)
  createdAt DateTime @default(now())

  @@index([cpf])
}

model DebitInvalidLog {
  id               String   @id @default(cuid())
  createdAt        DateTime @default(now())
  // request
  cpf              BigInt   @db.BigInt
  debitoReais      Decimal  @db.Decimal(12, 2)
  descricaoEstorno String?
  dataMov          DateTime
  // response
  nome             String?
  documento        String?
  saldoPreEstorno  Decimal? @db.Decimal(14, 2)
  pontosEstornados Int?
  codigoResposta   Int
  nunota           String?
  rawResponse      Json?

  @@index([cpf])
  @@index([codigoResposta])
  @@index([dataMov])
}
